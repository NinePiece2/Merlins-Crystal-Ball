pages:

home: should show all of your characters: can click on each one and the different levels for the character that have been uploaded are shown in a grid with the ability to add more, button to view the pdf that has been uploaded and view a preview of the data being extracted. Old files can be overwitted. When a character is created they can be added to an existing campaign.

DM View: Select a campaign to view and toggle at the top somewhere to select current level, persistent as a db entry for each use. Should be able to see all of the characters in that campaign in that level and important data should be shown in this page, each character can be selected to view more information. Missing users are shown with an error if that level doesn't exist for that user.

Settings Page: Users should be able to change their name, username, email and password.
Admin Settings Page: Admin users should be able to see users, edit them, add new users and create campaigns.

Files are to be uploaded to a local minio file store.

when a user gives their character sheet for their character data should be parsed from the character sheet. A sample character sheet is given, there are fillable components of the pdf such as 'CLASS LEVEL' which in the sample is given as 'Barbarian 3' or 'MaxHP' which has a value of '35'
